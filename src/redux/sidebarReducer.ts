

const initialState = {}



export const sidebarReducer = (state:any, action:ActionSidebarType) => {
    switch (action.type) {
        case "XXX" : {
            return state
        }
        default : {
            return state
        }
    }
}



type ActionSidebarType = any